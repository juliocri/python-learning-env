# Requirements

* Docker
* docker-compose

# Installation

* If you have juyter notes already and you want to use them:
  * ```$ cp $NOTES_PATH ./notes```

* ```$ docker-compose build ```
* ```$ docker-compose up -d```

* http://localhost:8888

